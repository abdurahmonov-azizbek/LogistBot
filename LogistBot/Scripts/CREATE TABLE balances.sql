CREATE TABLE IF NOT EXISTS CompanyBalance(
    id BIGINT PRIMARY KEY NOT NULL,
    balance FLOAT NOT NULL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS DriverBalance(
    id BIGINT PRIMARY KEY NOT NULL,
    balance FLOAT NOT NULL DEFAULT 0
)